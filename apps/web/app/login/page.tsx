// Filled in by ScrumAgent-jnf (Login screen — Google OAuth-style entry).
// This page does NOT wrap in <AppShell> — see app/login/layout.tsx.
export default function LoginPage() {
  return (
    <div className="page">
      <h1 className="page-title">Login placeholder</h1>
    </div>
  );
}
